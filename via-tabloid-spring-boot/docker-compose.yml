services:
  postgres:
    image: postgres:latest
    container_name: postgres_via_tabloid
    environment:
      POSTGRES_DB: via_tabloid
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - ./scripts/init.sql:/docker-entrypoint-initdb/init.sql
      - ./db_data:/var/lib/postgresql/
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USERNAME}" ]
      interval: 5s
      timeout: 5s
      retries: 5


  via-tabloid-spring-boot:
    build:
      context: .. # two levels up to include contracts
      dockerfile: ./via-tabloid-spring-boot/Dockerfile
    container_name: via-tabloid-spring-boot
    ports:
      - "8080:8080"
    environment:
      DB_URL: ${DB_URL_DOCKER}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    depends_on:
      postgres:
        condition: service_healthy